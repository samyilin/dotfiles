#!/bin/sh

# Removing git setup is up to the user, it's rather trivial and don't
# need my intervention.

printf "git config won't be removed. Please look into ~/.gitconfig \n"
printf "to manually remove git settings.\n"
