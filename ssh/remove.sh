#!/bin/sh

# Removing SSH setup is up to the user, it's rather trivial and don't
# need my intervention.

printf "SSH config won't be removed. Please look into ~/.ssh folder \n"
printf "to manually remove SSH settings.\n"
