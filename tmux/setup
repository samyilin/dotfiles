#!/bin/bash
if ! command -v tmux &> /dev/null; then
    echo 'tmux is not installed, skipping...'
else
    find $HOME/.tmux.conf && mv -f $HOME/.tmux.conf $HOME/.tmux.conf.bak
    ln -sf "$PWD/.tmux.conf" "$HOME/.tmux.conf"
fi
