# delta config.
git:
  diffRenderers:
    - colorArg: always
      command: delta --paging=never --line-numbers --hyperlinks --hyperlinks-file-link-format="lazygit-edit://{path}:{line}" --side-by-side --line-numbers-right-format = "│ "
